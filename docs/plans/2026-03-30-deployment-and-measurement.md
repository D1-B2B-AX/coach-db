# 2026-03-30 배포 준비 + 측정 설계

## 1. Sentry 에러 트래킹

### 설계 결정
- **SDK**: `@sentry/nextjs` (App Router 공식 지원)
- **플랜**: 무료 (월 5,000 이벤트, 매니저 40명 규모에 충분)
- **제외**: 성능 모니터링(tracing), Session Replay — 불필요

### 구현 완료
- [x] 클라이언트/서버/엣지 3개 런타임 초기화
- [x] `withSentryConfig()` 래핑, CSP에 Sentry 도메인 추가
- [x] `error.tsx`, `global-error.tsx`에 `captureException` 연결
- [x] 삼성 동기화 API에 Cron Monitor 연동 (`captureCheckIn`: in_progress → ok/error)
- [x] Railway 환경변수 설정 + 배포 확인

### 알림 규칙 (Sentry 대시보드)

| 규칙 | 조건 | 액션 |
|------|------|------|
| Uptime Monitor | `/api/health` 5분 간격, 3회 연속 실패 | 자동 이슈 생성 |
| 새 에러 발생 | 새 Issue 생성 (production, error) | Slack 알림 |
| 에러 폭증 | 1시간 내 에러 20건 이상 (warning: 5건) | Slack 알림 |
| Cron Monitor | `samsung-schedule-sync` 매월 마지막주 월요일 미실행 | 자동 missed 알림 |

### 환경변수

| 변수 | 용도 |
|------|------|
| `NEXT_PUBLIC_SENTRY_DSN` | 클라이언트+서버 DSN |
| `SENTRY_AUTH_TOKEN` | 소스맵 업로드 |
| `SENTRY_ORG` | 조직 slug |
| `SENTRY_PROJECT` | 프로젝트 slug |

---

## 2. 보안 헤더

- [x] **HSTS**: `max-age=63072000`, `includeSubDomains`, `preload`
- [x] **CSP**: `default-src 'self'`
  - `script-src`: self + accounts.google.com + sentry-cdn.com
  - `style-src`: self + unsafe-inline + cdn.jsdelivr.net (Pretendard)
  - `connect-src`: self + Google OAuth/API + sentry.io
  - `frame-src`: accounts.google.com
- [x] 기존 유지: X-Frame-Options, X-Content-Type-Options, Referrer-Policy, X-DNS-Prefetch-Control

---

## 3. CI/CD

- [x] GitHub Actions CI: push/PR → lint → tsc --noEmit → build
- [x] 삼성 동기화 cron: 매주 월요일 00:00 UTC, 마지막 주만 동기화
- [x] Railway 배포 자동화 (GitHub App 연동 완료)

---

## 4. DB 백업

- [x] Railway 자동 백업 확인
- [x] Cloudflare R2 백업 (매일 KST 03:00, 30일 보관)
- [ ] 복원 테스트: 스테이징 DB에 백업 복원 1회 수행

---

## 5. 코치뷰 UX 개선

- [x] 프로필 미입력 안내: fields 비어있으면 구글폼 안내 팝업 자동 표시
- [x] 활동 중단 신청: 프로필 모달 하단에 사유 입력 → status inactive 처리
- [x] 과정명 정리: 내부 태그 제거 ([부가세별도], (B2B), _연월 등)
- [x] 불가 처리 개선: 확정 일정 있는 날 불가 불가능, 불가 버튼 빨간색 강조
- [x] UI 정리: 나가기 버튼 제거, 마지막 저장 위치 이동, 장소 없으면 숨김
- [x] 달력 범위 제한: 당월~2개월 후, 과거 이동 불가

---

## 6. 측정 프레임워크

| 문제 | 기대 효과 | 지표 | 소스 |
|------|----------|------|------|
| 기억/동료 의존으로 코치 탐색 | 풀 전체 활용 | 매니저당 고유 코치 조회 수 | 앱 자동 |
| 시트에서 수동 일정 확인 | 즉시 확인 | 배정 소요 시간 체감 | 설문 |
| 수동 연락 발송 | 연락 간소화 | 연락처 복사 횟수 | 앱 자동 |
| 수동 연락 발송 | 연락 횟수 감소 | 확정까지 문자/메일 발송 횟수 | 설문 |
| 응답 대기/재연락 | 대기 시간 감소 | 배정까지 왕복 횟수 | 설문 |
| 기록 누락/이중 배정 | 사고 제로 | 이중 배정 건수 | 앱 자동 |
| 구인글 복사/수정 | 반복 제거 | — (앱 미지원) | — |
| 신규 코치 재입력 | 자동 등록 | 신청→등록 전환율 | 앱 자동 |

---

## 7. 설문조사 (월 1회)

**폼 제목:** 코치DB 사용 경험 설문
**폼 설명:** 코치DB 개선을 위한 월간 설문입니다. 2분이면 끝납니다.

### Q1. 이번 달 코치 배정 업무에 평균 얼마나 걸렸나요? (코치 1명 기준)
- 유형: 단일선택 (필수)
- 보기: 5분 이내 / 5~15분 / 15~30분 / 30분~1시간 / 1시간 이상 / 이번 달 배정 업무 없음

### Q2. 이번 달 코치 1명을 확정하기까지 문자/메일을 몇 회 보냈나요?
- 유형: 단일선택 (필수)
- 보기: 1~2회 / 3~5회 / 6~10회 / 10회 이상 / 이번 달 배정 업무 없음

### Q3. 코치DB 도입 전과 비교하면?
- 유형: 단일선택 (필수)
- 보기: 훨씬 빨라짐 / 조금 빨라짐 / 비슷함 / 오히려 느려짐 / 비교하기 어려움

### Q4. 이번 달 코치를 어떻게 찾았나요? (복수 선택)
- 유형: 복수선택 (필수)
- 보기: 코치DB 검색/필터 / 코치DB 대시보드 일정 확인 / 내가 아는 코치에게 직접 연락 / 동료에게 추천 요청 / 구글시트에서 검색 / 기타: ___

### Q5. 가장 유용한 기능은? (복수 선택)
- 유형: 복수선택 (필수)
- 보기: 코치 검색/필터 / 대시보드 일정 확인 / 연락처 바로 복사 / 투입 이력 조회 / 코치 신청 관리 / 아직 잘 모르겠음

### Q6. 이번 달 코치 배정 중 곤란했던 순간이 있었나요?
- 유형: 주관식 (선택)
- 힌트: 예) 원하는 분야 코치를 못 찾았다, 일정이 안 맞아서 여러 명 연락했다 등

### Q7. 추가되면 좋겠는 기능이나 개선 의견
- 유형: 주관식 (선택)

---

## 8. 버그리포트 (수시)

**폼 제목:** 코치DB 불편/오류 신고
**폼 설명:** 사소한 것도 좋아요. 30초면 됩니다.

### Q1. 지금 몇 시쯤이에요?
- 유형: 단일선택 (필수)
- 보기: 오전 (9~12시) / 오후 (12~15시) / 오후 (15~18시) / 저녁 이후

### Q2. 어떤 화면에서?
- 유형: 단일선택 (필수)
- 보기: 대시보드 / 코치 목록 / 코치 상세 / 관리자 / 동기화 / 기타

### Q3. 어떤 종류?
- 유형: 단일선택 (필수)
- 보기: 안 됨 (에러/오류) / 느림 / 헷갈림 (어디를 눌러야 할지 모르겠음) / 기타

### Q4. 무슨 일이 있었나요?
- 유형: 주관식 (필수)
- 힌트: 한 줄이라도 OK

### Q5. 스크린샷
- 유형: 파일 업로드 (선택)

---

## 보류 항목
- DB 백업 복원 테스트
- Sentry `onRouterTransitionStart` 경고 대응
